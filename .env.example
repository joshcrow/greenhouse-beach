# =============================================================================
# GREENHOUSE GAZETTE ENVIRONMENT CONFIGURATION
# =============================================================================
# Copy this file to .env and fill in your real values
# NEVER commit .env to git!
# =============================================================================

# Core identity
GREENHOUSE_SITE_NAME="Outer Banks Greenhouse"
GREENHOUSE_TIMEZONE="America/New_York"

# -----------------------------------------------------------------------------
# MQTT Broker
# -----------------------------------------------------------------------------
MQTT_HOST=mosquitto
MQTT_PORT=1883
MQTT_USERNAME=greenhouse
# Generate strong password: openssl rand -base64 24
MQTT_PASSWORD=YOUR_STRONG_MQTT_PASSWORD

# -----------------------------------------------------------------------------
# AI Narrative Engine (Google Gemini)
# -----------------------------------------------------------------------------
# Get key from: https://makersuite.google.com/app/apikey
GEMINI_MODEL=gemini-2.5-flash
GEMINI_API_KEY=YOUR_GEMINI_API_KEY

# -----------------------------------------------------------------------------
# Weather Intelligence (OpenWeatherMap)
# -----------------------------------------------------------------------------
# Get key from: https://openweathermap.org/api (One Call 3.0)
OPENWEATHER_API_KEY=YOUR_OPENWEATHER_API_KEY
LAT=36.022
LON=-75.720

# -----------------------------------------------------------------------------
# Email Dispatch (Gmail with App Password)
# -----------------------------------------------------------------------------
# Create App Password: Google Account > Security > 2FA > App passwords
SMTP_SERVER=smtp.gmail.com
SMTP_PORT=465
SMTP_USER=your-email@gmail.com
SMTP_PASSWORD=YOUR_GMAIL_APP_PASSWORD
SMTP_FROM="Greenhouse Gazette <your-email@gmail.com>"
SMTP_TO=recipient@example.com

# -----------------------------------------------------------------------------
# Paths (usually don't change)
# -----------------------------------------------------------------------------
STATUS_PATH=/app/data/status.json
DB_PATH=/app/data/db/greenhouse.db

# -----------------------------------------------------------------------------
# App Security
# -----------------------------------------------------------------------------
# Generate: openssl rand -base64 32
SECRET_KEY=YOUR_SECRET_KEY
